BUNKER 产品针对用户的开发提供了 CAN 接口， 用户可通过该接口对车体进行指令控制。

BUNKER 产品中 CAN 通信标准采用的是 CAN2.0B 标准， 通讯波特率为 500K， 报文格式采用 MOTOROLA 格式。通过外部 CAN 总线接口可以控制底盘的移动的线速度以及旋转的角速度；BUNKER 会实时反馈当前的运动状态信息以及 BUNKER 底盘的状态信息等。

协议包含系统状态回馈帧、运动控制回馈帧、控制帧，协议内容具体如下：

系统状态回馈指令包含了当前车体状态回馈、控制模式状态回馈、电池电压回馈以及故障回馈，协议内容如表 3.1 所示。

### 表格 3.1 BUNKER底盘系统状态回馈帧

**帧信息：**
| 指令名称 | 发送节点 | 接收节点 | ID | 周期（ms） | 接收超时(ms) | 数据长度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 系统状态回馈指令 | 线控底盘 | 决策控制单元 | 0x211 | 20ms | 无 | 0x08 |

**数据内容：**
| 位置 | 功能 | 数据类型 | 说明 |
| :--- | :--- | :--- | :--- |
| byte [0] | 当前车体状态 | unsigned int8 | 0x00 系统正常<br>0x01 紧急停车模式<br>0x02 系统异常 |
| byte [1] | 模式控制 | unsigned int8 | 0x00 待机模式<br>0x01 CAN指令控制模式<br>0x03 遥控控制模式 |
| byte [2] | 电池电压高八位 | unsigned int16 | 实际电压 X 10 (精确到0.1V) |
| byte [3] | 电池电压低八位 | unsigned int16 | 同上 |
| byte [4] | 保留 | -- | 0x00 |
| byte [5] | 故障信息 | unsigned int8 | 详见故障信息说明表 (表3.2) |
| byte [6] | 保留 | -- | 0x00 |
| byte [7] | 计数校验（count）| unsigned int8 | 0\~255循环计数，每发送一条指令计数加一次 |

### 表格 3.2 故障信息说明表

| 字节 | 位 | 含义 |
| :--- | :--- | :--- |
| byte [5] | bit [0] | 电池欠压故障 |
| | bit [1] | 电池欠压警告 |
| | bit [2] | 遥控器失联保护（0：正常，1：遥控器失联） |
| | bit [3] | 驱动1通讯故障 (0：无故障，1：故障) |
| | bit [4] | 驱动2通讯故障 (0：无故障，1：故障) |
| | bit [5] | 预留，默认0 |
| | bit [6] | 预留，默认0 |
| | bit [7] | 预留，默认0 |

-----

运动控制回馈帧指令包含了当前车体的运动线速度、运动角速度回馈，协议具体内容如表 3.3 所示。

### 表格 3.3 运动控制回馈帧

**帧信息：**
| 指令名称 | 发送节点 | 接收节点 | ID | 周期（ms） | 接收超时(ms) | 数据长度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 运动控制回馈指令 | 线控底盘 | 决策控制单元 | 0x221 | 20ms | 无 | 0x08 |

**数据内容：**
| 位置 | 功能 | 数据类型 | 说明 |
| :--- | :--- | :--- | :--- |
| byte [0] | 移动速度高八位 | signed int16 | 实际速度 X 1000 (单位0.001m/s) |
| byte [1] | 移动速度低八位 | signed int16 | 同上 |
| byte [2] | 旋转速度高八位 | signed int16 | 实际内转角 X 100 (单位0.001rad/s) |
| byte [3] | 旋转速度低八位 | signed int16 | 同上 |
| byte [4] | 保留 | - | 0x00 |
| byte [5] | 保留 | - | 0x00 |
| byte [6] | 保留 | - | 0x00 |
| byte [7] | 保留 | - | 0x00 |

-----

控制帧包含了线速度控制开度、角速度控制开度以及检验和，其具体协议内容如表 3.4 所示。

### 表格 3.4 运动控制帧

**帧信息：**
| 指令名称 | 发送节点 | 接收节点 | ID | 周期（ms） | 接收超时(ms) | 数据长度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 运动控制指令 | 决策控制单元 | 线控底盘 | 0x111 | 20ms | 无 | 0x08 |

**数据内容：**
| 位置 | 功能 | 数据类型 | 说明 |
| :--- | :--- | :--- | :--- |
| byte [0] | 线速度高八位 | signed int16 | 车体行进速度，单位mm/s, 值域[-1500,1500] |
| byte [1] | 线速度低八位 | signed int16 | 同上 |
| byte [2] | 角速度高八位 | signed int16 | 车体旋转角速度，单位0.001rad/s, 值域[-1000,1000] |
| byte [3] | 角速度低八位 | signed int16 | 同上 |
| byte [4] | 保留 | - | 0x00 |
| byte [5] | 保留 | - | 0x00 |
| byte [6] | 保留 | - | 0x00 |
| byte [7] | 保留 | - | 0x00 |

#### 示例数据

以下数据仅供测试使用 (测试前需要先把控制模式设定在指令模式)

**1. 小车以 0.15m/s 的速度前进**
| byte [0] | byte [1] | byte [2] | byte [3] | byte [4] | byte [5] | byte [6] | byte [7] |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| 0x00 | 0x96 | 0x00 | 0x00 | 0x00 | 0x00 | 0x00 | 0x00 |

**2. 小车以 0.2rad/s 旋转**
| byte [0] | byte [1] | byte [2] | byte [3] | byte [4] | byte [5] | byte [6] | byte [7] |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| 0x00 | 0x00 | 0x00 | 0xc8 | 0x00 | 0x00 | 0x03 | 0x00 |

-----

模式设定帧用于设定终端的控制接口，其具体协议内容如表 3.5 所示。

### 表格 3.5 控制模式设定帧

**帧信息：**
| 指令名称 | 发送节点 | 接收节点 | ID | 周期（ms） | 接收超时(ms) | 数据长度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 控制指令 | 决策控制单元 | 底盘节点 | 0x421 | 20ms | 500ms | 0x01 |

**数据内容：**
| 位置 | 功能 | 数据类型 | 说明 |
| :--- | :--- | :--- | :--- |
| byte [0] | CAN控制使能 | unsigned int8 | 0x00 待机模式<br>0x01 CAN指令模式 |

> **注[1]控制模式说明：** BUNKER 在遥控器不上电的情况下，控制模式默认是待机模式，需要切换到指令模式才能发送运动控制指令。若打开遥控器，遥控器拥有最高权限，可以屏蔽指令的控制。当遥控器切换到指令模式时，还是需要先发送控制模式设定指令才能响应速度指令。

-----

状态置位帧用于清除系统错误，其具体协议内容如表 3.6 所示。

### 表格 3.6 状态置位帧

**帧信息：**
| 指令名称 | 发送节点 | 接收节点 | ID | 周期（ms） | 接收超时(ms) | 数据长度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 控制指令 | 决策控制单元 | 底盘节点 | 0x441 | 无 | 无 | 0x01 |

**数据内容：**
| 位置 | 功能 | 数据类型 | 说明 |
| :--- | :--- | :--- | :--- |
| byte [0] | 错误清除指令 | unsigned int8 | 0x00 清除全部错误<br>0x01 清除1号电机错误<br>0x02 清除2号电机错误 |

-----

除了底盘的状态信息会进行反馈以外，底盘反馈的信息还包括电机数据和传感器数据。

### 表格 3.7 电机转速电流位置信息反馈

**帧信息：**
| 指令名称 | 发送节点 | 接收节点 | ID | 周期（ms） | 接收超时(ms) | 数据长度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 电机驱动器高速信息反馈帧 | 线控底盘 | 决策控制单元 | 0x251\~0x252 | 20ms | 无 | 0x08 |

**数据内容：**
| 位置 | 功能 | 数据类型 | 说明 |
| :--- | :--- | :--- | :--- |
| byte [0] | 电机转速高八位 | signed int16 | 电机当前转速 单位RPM |
| byte [1] | 电机转速低八位 | signed int16 | 同上 |
| byte [2] | 保留 | - | 0x00 |
| byte [3] | 驱动器温度低八位 | - | 单位1℃ |
| byte [4] | 保留 | - | 0x00 |
| byte [5] | 驱动器状态 | - | 详见表3.9 |
| byte [6] | 保留 | - | 0x00 |
| byte [7] | 保留 | - | 0x00 |

### 表格 3.8 电机温度电压及状态反馈

**帧信息：**
| 指令名称 | 发送节点 | 接收节点 | ID | 周期（ms） | 接收超时(ms) | 数据长度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 电机驱动器低速信息反馈帧 | 线控底盘 | 决策控制单元 | 0x261\~0x262 | 无 | 无 | 0x08 |

**数据内容：**
| 位置 | 功能 | 数据类型 | 说明 |
| :--- | :--- | :--- | :--- |
| byte [0] | 保留 | -- | 0x00 |
| byte [1] | 保留 | -- | 0x00 |
| byte [2] | 驱动器温度高八位 | signed int16 | 单位1℃ |
| byte [3] | 驱动器温度低八位 | signed int16 | 同上 |
| byte [4] | 保留 | - | 0x00 |
| byte [5] | 驱动器状态 | unsigned int8 | 详见表 3.9 |
| byte [6] | 保留 | - | 0x00 |
| byte [7] | 保留 | - | 0x00 |

### 表格 3.9 驱动器状态

| 字节 | 位 | 含义 |
| :--- | :--- | :--- |
| byte [5] | bit [0] | 电源电压是否过低（0：正常，1：过低） |
| | bit [1] | 电机是否过温（0：正常，1：过温） |
| | bit [2] | 保留 |
| | bit [3] | 保留 |
| | bit [4] | 保留 |
| | bit [5] | 保留 |
| | bit [6] | 保留 |
| | bit [7] | 保留 |

### 表格 3.10 里程计反馈帧

**帧信息：**
| 指令名称 | 发送节点 | 接收节点 | ID | 周期（ms） | 接收超时（ms） | 数据长度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 里程计信息反馈 | 线控底盘 | 决策控制单元 | 0x311 | 20ms | 无 | 0x08 |

**数据内容：**
| 字节 | 描述 | 数据类型 | 说明 |
| :--- | :--- | :--- | :--- |
| byte [0] | 左轮里程计最高位 | signed int32 | 底盘左轮里程计反馈， 单位：mm |
| byte [1] | 左轮里程计次高位 | signed int32 | 同上 |
| byte [2] | 左轮里程计次低位 | signed int32 | 同上 |
| byte [3] | 左轮里程计最低位 | signed int32 | 同上 |
| byte [4] | 右轮里程计最高位 | signed int32 | 底盘右轮里程计反馈， 单位：mm |
| byte [5] | 右轮里程计次高位 | signed int32 | 同上 |
| byte [6] | 右轮里程计次低位 | signed int32 | 同上 |
| byte [7] | 右轮里程计最低位 | signed int32 | 同上 |

### 表格 3.11 遥控器信息反馈

**帧信息：**
| 指令名称 | 发送节点 | 接收节点 | ID | 周期（ms） | 接收超时(ms) | 数据长度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 遥控器信息反馈 | 线控底盘 | 决策控制单元 | 0x241 | 20ms | 无 | 0x08 |

**数据内容：**
| 位置 | 功能 | 数据类型 | 说明 |
| :--- | :--- | :--- | :--- |
| byte [0] | 遥控SW反馈 | unsigned int8 | bit[0-1]: SWA ：2-上档 3-下档<br>bit[2-3]: SWB：2-上档 1-中档 3-下档<br>bit[4-5]: SWC：2-上档 1-中档 3-下档<br>bit[6-7]: SWD：2-上档 3-下档 |
| byte [1] | 右边拨杆左右 | unsigned int8 | 值域：[-100,100] |
| byte [2] | 右边拨杆上下 | unsigned int8 | 值域：[-100,100] |
| byte [3] | 左边拨杆上下 | unsigned int8 | 值域：[-100,100] |
| byte [4] | 左边拨杆左右 | unsigned int8 | 值域：[-100,100] |
| byte [5] | 左边旋钮VRA | unsigned int8 | 值域：[-100,100] |
| byte [6] | 保留 | -- | 0x00 |
| byte [7] | 计数校验 | unsigned int8 | 0-255循环计数 |